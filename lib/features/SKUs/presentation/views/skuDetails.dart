import 'package:flutter/material.dart';

class SKUDetails extends StatelessWidget {
  const SKUDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
